// ignore_for_file: constant_identifier_names

enum ViewState { Idle, Busy }

enum UserType { employee, vendor }

enum LoadMoreStatus { LOADING, STABLE }

enum FilterData { NUMBEROFREVIEW, BESTREVIEWS, NONVETTED }

enum MessageType { sent, received }

enum LikeType { on, off }

enum Vendor { Google, Unverified, Verifed, None }
